name: kollab_theme
description: A new Flutter package project.
version: 0.0.4
homepage: https://github.com/bitmio-labs/kollab-theme

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  json_serializable: ^3.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/
